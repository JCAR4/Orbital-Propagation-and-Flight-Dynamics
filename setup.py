from setuptools import setup, find_packages

setup(
    name="orbital_propagation_and_flight_dynamics",
    version="0.1.0",
    description="A package for orbital propagation and flight dynamics.",
    author="Your Name",
    author_email="your.email@example.com",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here, e.g. 'numpy', 'scipy'
    ],
    python_requires='>=3.7',
)